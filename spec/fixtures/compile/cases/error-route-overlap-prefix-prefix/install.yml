schema_version: 2
pack: .
runtime: hermes
routes:
  - id: short-prefix
    match: {session_key: {prefix: dummy-}}
    allowed_modes: [public]
    switching: deny
    state_domain: dummy-a
  - id: long-prefix
    match: {session_key: {prefix: dummy-private-}}
    allowed_modes: [public]
    switching: deny
    state_domain: dummy-b
